const state = {
    //运单号
    carriageId: ""
}
const mutations = {
    carriageIdGet(state,object) {
        state.carriageId = object.value;
    }
}

export default {
    //namespaced: true|命名空间，解决不同模块命名冲突的问题。
    namespaced: true,
    state,
    mutations
};